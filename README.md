# WebBridge

GitHub Pages frontend for the Android proxy project.

Edit `script.js` and replace `https://YOUR-VERCEL-DOMAIN.vercel.app` with your Vercel backend URL.

Do not put a secret API key in this public frontend.

Enable GitHub Pages from repository Settings → Pages.